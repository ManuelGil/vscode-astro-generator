/**
 * View identifiers for the Angular VSCode extension.
 * These IDs are used for TreeViews and other VSCode UI components.
 */
export enum ViewIds {
  ListComponentsView = 'listComponentsView',
  FeedbackView = 'feedbackView',
}
